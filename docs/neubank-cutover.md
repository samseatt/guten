# Neubank domain cutover

Activated September 19, 2026 UTC; user visual acceptance and local recursive DNS convergence pending.

The candidate `deploy/domains.neubank.json` preserves Guten and Portal and adds only `neubank.org` for published site `neubank`, with `www.neubank.org` redirected to the canonical hostname. AWS remains the content master; no database import or content edit is involved.

## Private acceptance

The private AWS preview passed root-to-landing routing (`/what_neubank/neubank_something_new`), canonical URLs, two loaded images, menu navigation, direct reload, and rejection of editorial/cross-site/spoofed-header API requests. The screenshot was visually checked. Evidence: `artifacts/neubank-preview-browser-20260919-verified/`. The initial test completed its assertions but exposed a teardown race; the shared test now waits for in-flight route handlers before closing the context, and the rerun exited successfully.

The shared public test accepts `GUTEN_PUBLIC_ORIGIN=https://neubank.org` and `GUTEN_PUBLIC_SITE=neubank`; omit `GUTEN_PREVIEW_BASE` for real public HTTPS verification after cutover. Use a new `GUTEN_PUBLIC_ARTIFACTS` directory each run.

## DNS preparation

Keep Namecheap BasicDNS. Use Domain List > Manage > Advanced DNS, not Custom DNS nameserver fields. Intended records after preparation and rollback review:

| Type | Host | Value |
| --- | --- | --- |
| A | @ | 52.54.75.169 |
| CNAME | www | neubank.org |

Preserve mail/TXT and unrelated records. Replace conflicting website/URL Redirect records only after recording their exact configuration. Public DNS currently reports root A 162.255.119.142 and www CNAME parkingpage.namecheap.com, TTL 1800 seconds. Sam confirmed the original console records: `www` is a CNAME to `parkingpage.namecheap.com.` with TTL 30 minutes; `@` is an Unmasked URL Redirect to `http://www.neubank.org/`, with no TTL displayed. The observed root A is generated by that forwarding service. For rollback, restore these exact record types and values, not a manually entered A record for the forwarding IP. No explicit root AAAA or CAA was observed. Five mail-forwarding MX records and the SPF TXT were recorded in `artifacts/neubank-dns-before.json`.

After the user saves the records, verify authoritative DNS and mail preservation, activate the prepared release, check trusted certificates and HTTP/www redirects, rerun public acceptance for Neubank and Guten, and obtain user visual acceptance. DNS caches may retain the previous answer for its TTL. Rollback requires restoring original Namecheap website records and deliberately reverting the application release if needed; never restore the database for a domain-routing rollback.

## Optional Foundry hostname

The old Cloudflare Pages project can separately serve `foundry.guten.ink`. Add that name under Workers & Pages > the existing Guten Pages project > Custom domains > Set up a domain. Let Pages create/confirm the corresponding CNAME to guten.pages.dev. A DNS-only manual CNAME without the Pages custom-domain association is insufficient. Leave root, www and Portal routing unchanged. This hostname is independent of the AWS application and database; it is not a Guten Portal preview or editing endpoint.

References: https://developers.cloudflare.com/pages/configuration/custom-domains/ and https://www.namecheap.com/support/knowledgebase/article.aspx/319/78/how-can-i-set-up-an-a-address-record-for-my-domain/

The immutable release `guten-neubank-20260919-01` is staged on the app host and passed bundle verification. The private preview containers and SSH tunnel have been removed. Public activation awaits confirmation of the two Namecheap record changes.

## Activation result

Sam saved the A/CNAME records and kept BasicDNS. Authoritative DNS confirms root A 52.54.75.169 and www CNAME neubank.org. Nameservers, all five MX records and SPF TXT match the baseline; no root AAAA remains. Release `guten-neubank-20260919-01` applied successfully and passed its trusted public HTTPS smoke checks.

Both Neubank names have valid Let's Encrypt certificates (expiry December 18, 2026). Chrome acceptance with hostname resolution explicitly pinned to the AWS IP passed landing, images, canonical URL, navigation/reload, API isolation, www redirect and absence of browser errors. Certificate verification remained enabled. HTTP and www redirects preserve the page path. Guten's normal public browser suite passed, including Portal sign-in availability and unauthenticated API rejection.

The Mac's normal resolver still returned the old Namecheap forwarding IP 162.255.119.142; the unpinned Neubank test timed out. This is not counted as a successful normal-DNS browser test. Repeat that test with a new artifact directory after cache expiry, and obtain user visual acceptance. No machine-wide DNS overrides were installed.

Evidence: `artifacts/neubank-deployment-20260919.log`, `neubank-dns-after.json`, `neubank-origin-tls-20260919.json`, `neubank-origin-browser-results.json`, `neubank-origin-browser.png`, and `guten-after-neubank-browser-20260919/`. Temporary preview containers/tunnel were removed before activation. No database changes or new paid infrastructure were required.
